# Copy to .env and adjust as needed
# Database (matches docker-compose postgres)
DATABASE_URL="postgresql://gearhead:gearhead_secret@localhost:5432/gearhead_db"

# Redis (for cache, rate limiting, queues)
REDIS_URL="redis://localhost:6379"

# API
PORT=3000
