import { registerRootComponent } from 'expo';
import App from './App';

/**
 * Entry point for the Expo app.
 * Uses registerRootComponent so it works across native and web.
 */
registerRootComponent(App);

